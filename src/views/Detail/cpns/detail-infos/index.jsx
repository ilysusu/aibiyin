import React from 'react';
import {InfoWrapper} from "@/views/Detail/cpns/detail-infos/style";

const DetailInfos = props => {
  return (
    <InfoWrapper>
      DetailInfos
    </InfoWrapper>
  );
};

export default React.memo(DetailInfos);
