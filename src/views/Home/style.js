import { styled } from "styled-components";

export const HomeWrapper = styled.div`
  > .container {
    width: 1200px;
    min-height: 600px;
    margin: 0 auto;
  }
  .good-price {
    margin-top: 30px;
  }
  .room-list {
    display: flex;
    flex-wrap: wrap;
    margin: 0 -8px;
  }

`
